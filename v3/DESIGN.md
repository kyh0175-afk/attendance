# 코스모스 출석 v3 설계서

> 작성: 2026-07-21 (Claude Code) · 검토: 사자님
> 목적: **입실 스캔 후 무단이탈("찍튀") 방지** + 학생 개별 로그인 도입 + v2 보안 잔여과제(PII 읽기) 해소
> 근거: 7/21 딥리서치(상용 관리형독서실·법률·배포 102-agent 검증) + 실측(심야 평균 43.6명/최대 58명)

---

## 0. 요약 (한 문단)

v3는 출석의 정의를 "입실 1회"에서 **"입실+퇴실 2포인트"**로 바꾼다. 학생은 학번+PIN으로 로그인하는 **PWA**(홈 화면 설치형 웹앱)를 쓰고, 입실은 기존 QR, 퇴실은 **교사가 마감 전 발급하는 랜덤 코드**(유효 10분)를 입력한다. 문 앞 **키오스크(학생증 바코드 스캔, 옵셔널)**가 생기면 코드 없이 스캔만으로 입/퇴실이 된다. 입실은 있는데 퇴실이 없는 기록은 `퇴실미확인`으로 자동 마킹되어 관리자 통계에 잡힌다. 도입 첫 2주는 **측정 모드**(제재 없음, 통계만)로 운영해 실태를 확인한 후 룰을 확정한다.

## 1. 배경과 문제

- 현행(v2): 교사 QR 세션 → 학생이 폰으로 스캔 → 학번 입력 → 출석. **입실 1회 확인뿐**이라 스캔 직후 이탈해도 출석 처리.
- 운영 사실: 입실 후 **폰 제출**(통짜 박스, 슬롯 없음) → 자습 → 종료 시 폰 수령. 자유좌석. 심야 최대 58명.
- 상용 검증(딥리서치): 관리형 독서실 업계 표준 = **선언형 체크**(학생이 직접 찍는 입/퇴실/외출 + 미선언 부재를 예외로 플래그). 센서 자동감지는 상용에도 없음 → v3 방식이 검증된 패턴.

### 기각된 대안 (재제안 방지)
| 대안 | 기각 사유 |
|---|---|
| GPS/위치 앱 | 위치정보법(무동의 수집 = 5년/5천만원, 학생 **본인** 동의 필수 — 학부모 동의 갈음 불가, 2024 서울행법). 비행기모드로 회피 가능. 폰 두고 이탈은 원리상 못 잡음. 네이티브 앱 필수 |
| 중간 랜덤 재인증 | 자습 중 전원 폰 꺼내기 = 프로그램 취지 훼손. 폰 제출 정책과 충돌. 사자님 기각 |
| 좌석맵 점검 | 자유좌석이라 불성립 |
| 폰 슬롯 대조 | 보관함이 통짜 박스(번호 슬롯 아님) |
| 지문 인증 | 생체정보 = 민감정보, 별도 동의+암호화 의무. 학교 부담 과대 |
| 카드 신규 제작 | 제작 여건 없음. 대신 **기존 학생증 바코드** 활용 |
| 네이티브 앱 | 단일 학교 전용 앱은 App Store 공개 출시 거부(가이드라인 3.2). GPS 기각으로 필요성도 소멸 → PWA |

### 설계 원칙 (인권위 2025-08 결정 반영)
이탈 관리는 **기록·집계·알림까지만**. 물리적 봉쇄나 "이탈 불가" 강제로 설계하지 않는다. 정당한 이탈 경로(조퇴 처리)를 항상 제공한다. 자습 참여의 자발성 전제를 유지한다.

## 2. 사용자 흐름

### 2-1. 학생 (PWA — 폰)
```
[최초 1회] 접속 → 학번+초기PIN 로그인 → PIN 변경 → "홈 화면에 추가" 안내 → 설치
[입실]    자습실 도착 → 교사 QR 스캔(또는 앱 내 '입실' 버튼→세션 선택) → 입실 완료 → 폰 제출
[자습]    (폰 없음 — 시스템 관여 없음)
[퇴실]    종료 → 폰 수령 → 앱 '퇴실' 버튼 → 교사가 안내한 4자리 코드 입력 → 퇴실 완료
[조회]    홈 = 이번 달 출석 횟수/시간, 주간·월간 캘린더(v2 check 화면 계승), 수정·요일변경 요청
```
- 로그인 상태는 기기에 유지 → 재로그인 불필요. **본인 계정으로만 출석 가능** = 대리 출석하려면 폰을 통째로 맡겨야 하는데 폰 제출 정책과 충돌 → 자연 억제.
- 폰 미소지 학생: 키오스크(있으면) 또는 교사 화면 수동 처리(v2 수동출석 패턴 계승).

### 2-2. 교사 (태블릿/노트북 — 세션 단말)
```
[시작]  장소 선택 → 프로그램/교사명 → 세션 시작 (QR 표시 — v2 동일)
[진행]  실시간 명단: 입실 N명 (v2 동일)
[마감전] "퇴실 코드 발급" 버튼 (마감 10분 전 리마인드 배너) → 4자리 코드 대형 표시 → 칠판/구두 안내
[마감]  미퇴실 n명 표시 → 개별 확인: [조퇴 처리(사유)] / [수동 퇴실] / [그대로 마감]
       → 마감 확정 시 잔여 미퇴실 행 = '퇴실미확인' 자동 마킹
```
- 퇴실 코드: 세션별 랜덤 4자리, **발급 후 10분 유효**(1회 재발급 가능). 서버가 검증(세션·코드·유효시간·본인 입실기록 존재).
- 코드 원격공유(카톡 찍튀 2.0) 리스크: 짧은 유효창 + 본인 계정 필수 + "밖의 친구"는 자기 계정으로만 퇴실 가능(입실기록 없으면 거부) → 실질 억제. 잔여 리스크(중간이탈 후 마감 때 복귀해 직접 입력)는 **수용**(사자님 결정 7/21) — 재진입 비용 자체가 억제력이며, 반복 패턴은 통계로 드러남.

### 2-3. 키오스크 (옵셔널 모듈 — 문 앞 Windows 터치PC + 바코드 리더)
```
[입실] 학생증 바코드 스캔 → 삑 → 입실 완료 (화면에 이름 2초 표시)
[퇴실] 동일 스캔 → 퇴실 완료
```
- **코드 불필요**: 단말이 문 앞에 있으므로 스캔 = 현장 증명.
- 바코드 리더 = USB HID(키보드 에뮬) 가정 → 브라우저 입력창 포커스만으로 동작, 연동 코드 0.
- 처리량: 스캔 ~1.5초/명 → 60명 퇴실 ~2분 (넘버패드 대비 4배).
- 전제 확인(보류 중): ①바코드 내용=학번 여부 ②Edge 키오스크 모드 셋업 ③supabase.co 외부망 접근. **v3.0은 키오스크 없이도 완결** — 코드 방식이 기본, 키오스크는 가속 모듈.

### 2-4. 관리자 (v2 관리자 화면 계승 + 확장)
- 기존 8탭 유지. **분석 탭 확장**: 퇴실율, `퇴실미확인` 통계(학생별·기간별), 이상 패턴(반복 미퇴실자) 목록.
- 학생 PIN 초기화 버튼(명단 관리).

## 3. 인증 설계 ★결정 A

### A-1. Supabase Auth 사용 (권고)
- 학생 계정 = `{학번}@st.yubongsystem.com` 형식 가상 이메일 + PIN(6자리)을 비밀번호로. 이메일 발송 없음(확인 비활성).
- 온보딩: 관리자가 명단 업로드 시 계정 일괄 생성(초기 PIN = 생년월일 등) → 학생 첫 로그인 시 PIN 변경.
- **장점**: RLS를 `auth.uid()` 기반으로 진짜 잠금 — **v2에서 못 닫은 PII 읽기(명단·출석 전체 노출)가 이번에 닫힘**. 세션/토큰 갱신은 supabase-js가 자동. Realtime도 authenticated.
- 단점: 계정 수명주기 관리(전출입) 필요 — 명단 업로드에 통합.

### A-2. 자체 토큰 (v2 admin_sessions 패턴 확장)
- student_sessions 테이블 + 전 조회/쓰기를 SECURITY DEFINER RPC로.
- 장점: 단순. 단점: RPC 표면 급증, RLS 이점 없음, PII 읽기 잠금이 수작업.

**권고 = A-1.** 교사용은 **공용 교사 PIN**(개인계정 부담 회피) → teacher 역할 로그인으로 명단 접근. 관리자는 v2 토큰 체계 유지(또는 Auth admin 역할로 통합).

## 4. 데이터 설계

### 4-1. 기존 테이블 (유지 + 확장 — DB 연속성 원칙)
```
students        + pin 관련은 Auth로 이관(자체 저장 없음), 바코드ID 컬럼(학생증 바코드≠학번일 경우만)
attendance      + 퇴실시각 timestamptz null
                + 퇴실방식 text null  ('코드'|'키오스크'|'교사수동'|'조퇴')
                + 상태 확장: '출석' | '퇴실미확인' | '조퇴'  (기존 '출석' 유지 — 하위호환)
sessions        + 퇴실코드 text null, 퇴실코드만료 timestamptz null
```
- v2 데이터는 무변경 그대로 유효 (퇴실 컬럼 null = "v2 시절 기록"으로 해석).
- '중도이탈' 대신 **'퇴실미확인'** 명칭 채택: 단정 배제(코드 안내를 못 들은 경우 등 오탐 여지) + 인권위 원칙 부합. ★결정 B

### 4-2. 새 RPC (v2 보안 패턴 계승 — 전부 SECURITY DEFINER + 권한 최소화)
```
issue_exit_code(session_id)            교사 — 코드 생성·만료 설정
check_out(session_id, code)            학생(authenticated) — 검증 후 본인 행에 퇴실 기록
kiosk_check(barcode)                   키오스크 — 입실/퇴실 토글 (키오스크 전용 role/키)
finalize_session(session_id)           교사 — 마감 + 미퇴실 행 '퇴실미확인' 마킹
mark_early_leave(session_id, 학번, 사유) 교사 — 조퇴 처리
admin_reset_pin(token, 학번)            관리자 — PIN 초기화
```
- RLS: students/attendance SELECT = 본인 행(auth.uid 매핑) + teacher/admin 역할만 전체. anon SELECT **폐지** → v2 잔여과제 종결.

## 5. 프론트 아키텍처 ★결정 C

**빌드 없는 멀티파일** (권고): v2와 같은 바닐라 JS이되 14.5K줄 단일파일 반복을 피한다.
```
v3/
  index.html      학생 PWA (로그인·입실·퇴실·내 현황)
  teacher.html    교사 세션 단말
  kiosk.html      키오스크 모드 (대형 UI + 바코드 입력)
  admin.html      관리자
  assets/js/      공용 모듈 (sb.js = DAL, auth.js, ui.js) — <script type="module">
  assets/css/
  sw.js           서비스워커 (앱 셸 캐시 — 학교 와이파이 블립 대응)
  manifest.webmanifest
```
- 장점: 파일 소유권 분리(병렬 작업·리뷰 용이), 빌드 스텝 0 = GitHub Pages 배포 유지(git push = 배포), 사자님 단독 유지보수 가능.
- 대안(기각): Vite+프레임워크 — 빌드 체인 유지보수 부담이 학교 프로젝트에 과함.

## 6. 배포

- **도메인 = yubongsystem.com 서브도메인** (예: `cosmos.yubongsystem.com`) ★결정 D — **오픈 전 확정 필수** (PWA 설치·로그인 상태가 도메인에 종속, 이후 변경 시 전원 재설치).
- 연결: DNS CNAME → GitHub Pages (권고, 배포 흐름 유지) 또는 Lightsail 정적 서빙. Lightsail은 서버 필요 기능(챗봇 API 게이트웨이 등) 등장 시 예약.
- v2는 기존 주소에서 전환 완료까지 병행 운영.

## 7. 전환 계획 (방학 창구: ~8/16)

| 주차 | 기간 | 내용 |
|---|---|---|
| W1 | 7/22–7/27 | 설계 확정(결정 A~E) → 스켈레톤 + Auth + 학생 로그인/조회 화면 |
| W2 | 7/28–8/3 | 입실(QR 계승) + 퇴실 코드 전체 흐름 + 교사 화면 |
| W3 | 8/4–8/10 | 관리자 + 통계 + (여건 시) 키오스크 모드 + v2 병행 리허설 |
| W4 | 8/11–8/16 | **파일럿**: 방학 자습에서 실전 테스트(★방학 자습 QR 미운영 중 — 파일럿 가능 여부 사자님 확인) → 보정 |
| 개학 | 8/17~ | 정식 전환. **첫 2주 = 측정 모드**(퇴실 체크 운영하되 미퇴실 제재·통보 없음, 통계만) → 실태 보고 → 룰 확정 |

- 롤백: v2가 기존 주소에 그대로 있으므로 언제든 안내만 되돌리면 복귀. DB는 additive 변경만이라 비파괴.

## 8. 스코프 제외 (v3.1+ 후보)

- **외출/복귀 이벤트** (화장실 등 일시 이탈): 키오스크 정착 후
- **학부모 알림** (퇴실미확인 시 통보 — 상용 킬러 기능): 측정 모드 결과 보고 후 결정 ★결정 E
- 학생 챗봇 연동 (조회 RPC 공개): v3 오픈 후
- 좌석 신청 모듈: v2 것 그대로 사용 (마이그레이션 후순위)

## 9. 리스크 대장

| 리스크 | 등급 | 대응 |
|---|---|---|
| 중간이탈 후 마감 복귀 (미검출) | 수용 | 재진입 비용 자체가 억제. 반복 패턴은 통계 노출 |
| 퇴실 코드 원격 공유 | 낮음 | 10분 유효창 + 본인 계정 + 입실기록 필수 |
| 마감 시 미퇴실 다수 (운영 초기 혼선) | 중간 | 측정 모드 2주 + 교사 마감 화면에서 일괄 수동 처리 지원 |
| PWA 설치 이탈 (iOS 안내 번거로움) | 중간 | 설치 안내 화면 + 미설치여도 브라우저로 전 기능 동작 |
| 키오스크 바코드 ≠ 학번 | 낮음 | 매핑 컬럼으로 흡수 (테스트 보류 중, 옵셔널 모듈) |
| Auth 계정 수명주기 (전출입) | 낮음 | 명단 업로드에 생성/비활성 통합 |

---

## 부록: 사자님 결정 대기 목록

| # | 항목 | 옵션 | 권고 |
|---|---|---|---|
| A | 인증 방식 | Supabase Auth vs 자체 토큰 | **Auth** (PII 잠금 공짜) |
| B | 미퇴실 상태 명칭 | '퇴실미확인' vs '중도이탈' | **'퇴실미확인'** |
| C | 프론트 구조 | 빌드 없는 멀티파일 vs 단일파일 vs 프레임워크 | **멀티파일** |
| D | 도메인 | cosmos.yubongsystem.com (예시) | 서브도메인명만 정하면 됨 |
| E | 학부모 알림 | v3.0 제외 → 측정 후 결정 vs 처음부터 포함 | **측정 후 결정** |
| F | 파일럿 | 방학 자습에서 v3 실전 테스트 가능? | 가능하면 W4 파일럿 |
